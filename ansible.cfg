[defaults]
inventory        = ./inventory
library          = ./library
host_key_checking = False
retry_files_enabled = False
stdout_callback  = default
# Modules are standalone bash scripts that emit a single JSON line on stdout
# with {changed,failed,msg,rc,invocation}. Ansible parses that as the result.
